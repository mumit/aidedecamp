[pytest]
asyncio_mode = auto
# deploy/ holds standalone deployable services (e.g. deploy/republisher/)
# with their own dependency sets (Flask, etc.) independent of the aidedecamp
# package — never collect them as part of this package's test suite.
norecursedirs = deploy
